public class ObjectArrayTest {
    // creates an array of type InventoryItem, size 5, each element null
    // InventoryItem[] inventory = new InventoryItem[5];
    /*
    for (int i = 0; i < inventory.length; i++){
        inventory[i] = new InventoryItem();
    }
    each element is of type InventoryItem, so it has a description = "" and units = 0
    because InventoryItem has the same features
     */
}
